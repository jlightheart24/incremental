# Incremental Web

Web version scaffold using Vite + Phaser + TypeScript.

## Scripts
- `npm install`
- `npm run dev`
- `npm run build`
- `npm run preview`
- `npm test`

## Structure
- `src/domain/` - ported gameplay logic (renderer-agnostic)
- `src/scenes/` - Phaser scenes and UI
